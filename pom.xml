<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>de.tubyoub</groupId>
  <artifactId>velocitypteropower</artifactId>
  <version>0.9.6-alpha</version>
  <packaging>jar</packaging>

  <name>VelocityPteroPower</name>
  <description>A Velocity plugin for Pterodactyl power management with public API for developers</description>
  <url>https://github.com/Tubs-Pluginz/VelocityPteroPower</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>tubyoub</id>
      <name>TubYoub</name>
      <email>contact@tubyoub.de</email>
      <organization>Tubs-Pluginz</organization>
      <organizationUrl>https://github.com/Tubs-Pluginz</organizationUrl>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/Tubs-Pluginz/VelocityPteroPower.git</connection>
    <developerConnection>scm:git:ssh://github.com:Tubs-Pluginz/VelocityPteroPower.git</developerConnection>
    <url>https://github.com/Tubs-Pluginz/VelocityPteroPower/tree/main</url>
  </scm>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <maven.compiler.release>21</maven.compiler.release>

    <!-- Dependency versions -->
    <adventure.version>4.14.0</adventure.version>
    <minimessage.version>4.24.0</minimessage.version>
    <velocity.api.version>3.4.0-SNAPSHOT</velocity.api.version>
    <boostedyaml.version>1.3.1</boostedyaml.version>
    <jackson.databind.version>2.16.0</jackson.databind.version>

    <!-- Plugin versions -->
    <shade.plugin.version>3.5.1</shade.plugin.version>
    <compiler.plugin.version>3.11.0</compiler.plugin.version>
    <templating.plugin.version>1.0.0</templating.plugin.version>
    <site.plugin.version>3.12.1</site.plugin.version>
    <wagon.git.version>2.0.4</wagon.git.version>
    <doxia.markdown.version>1.9.1</doxia.markdown.version>
    <release.plugin.version>3.0.1</release.plugin.version>
    <deploy.plugin.version>3.1.2</deploy.plugin.version>
    <javadoc.plugin.version>3.6.3</javadoc.plugin.version>
    <source.plugin.version>3.3.1</source.plugin.version>
    <exec.plugin.version>3.1.0</exec.plugin.version>
    <central.plugin.version>0.9.0</central.plugin.version>
    <gpg.plugin.version>3.2.8</gpg.plugin.version>
  </properties>

  <profiles>
    <profile>
      <id>beta</id>
      <build>
        <resources>
          <resource>
            <directory>src/main/resources</directory>
            <filtering>true</filtering>
          </resource>
        </resources>
      </build>
    </profile>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <!-- Sign ALL artifacts -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>${gpg.plugin.version}</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <!-- Maven Central Portal Publishing - FIXED CONFIGURATION -->
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>${central.plugin.version}</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>true</autoPublish>
              <waitUntil>published</waitUntil>
              <deploymentName>VelocityPteroPower-${project.version}</deploymentName>
            </configuration>
          </plugin>

          <!-- Override javadoc plugin for release profile - API only -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>${javadoc.plugin.version}</version>
            <executions>
              <!-- Skip main javadoc in release mode to avoid Velocity API dependencies -->
              <execution>
                <id>attach-main-javadoc</id>
                <phase>none</phase>
              </execution>
              <!-- Only generate API javadoc in release mode -->
              <execution>
                <id>attach-api-javadoc</id>
                <phase>package</phase>
                <goals>
                  <goal>jar</goal>
                </goals>
                <configuration>
                  <classifier>javadoc</classifier>
                  <source>21</source>
                  <quiet>true</quiet>
                  <additionalJOption>-Xdoclint:none</additionalJOption>
                  <sourcepath>${project.basedir}/src/main/java</sourcepath>
                  <subpackages>de.tubyoub.vpp.api</subpackages>
                  <groups>
                    <group>
                      <title>Public API</title>
                      <packages>de.tubyoub.vpp.api*</packages>
                    </group>
                  </groups>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- Override source plugin for release profile - API sources only as main -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>${source.plugin.version}</version>
            <executions>
              <!-- Skip main sources in release mode -->
              <execution>
                <id>attach-main-sources</id>
                <phase>none</phase>
              </execution>
              <!-- Generate API sources as main sources jar -->
              <execution>
                <id>attach-api-sources-as-main</id>
                <phase>package</phase>
                <goals>
                  <goal>jar-no-fork</goal>
                </goals>
                <configuration>
                  <classifier>sources</classifier>
                  <includes>
                    <include>de/tubyoub/vpp/api/**</include>
                  </includes>
                </configuration>
              </execution>
            </executions>
          </plugin>

        </plugins>
      </build>
      <!-- Override dependencies to exclude SNAPSHOT versions when releasing -->
      <dependencies>
        <!-- Exclude Velocity API completely -->
        <dependency>
          <groupId>com.velocitypowered</groupId>
          <artifactId>velocity-api</artifactId>
          <version>${velocity.api.version}</version>
          <scope>provided</scope>
          <optional>true</optional>
          <exclusions>
            <exclusion>
              <groupId>*</groupId>
              <artifactId>*</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
        <!-- Keep other dependencies for API -->
        <dependency>
          <groupId>dev.dejvokep</groupId>
          <artifactId>boosted-yaml</artifactId>
          <version>${boostedyaml.version}</version>
          <optional>true</optional>
        </dependency>
        <dependency>
          <groupId>com.fasterxml.jackson.core</groupId>
          <artifactId>jackson-databind</artifactId>
          <version>${jackson.databind.version}</version>
          <optional>true</optional>
        </dependency>
        <dependency>
          <groupId>net.kyori</groupId>
          <artifactId>adventure-text-minimessage</artifactId>
          <version>${minimessage.version}</version>
        </dependency>
      </dependencies>
    </profile>
  </profiles>

  <build>
    <resources>
      <resource>
        <directory>${project.basedir}/src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
      <resource>
        <directory>${project.basedir}/src/main/java</directory>
        <filtering>true</filtering>
      </resource>
    </resources>

    <plugins>
      <!-- Shade the plugin (not the API) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${shade.plugin.version}</version>
        <configuration>
          <shadedArtifactAttached>true</shadedArtifactAttached>
          <shadedClassifierName>shaded</shadedClassifierName>
          <createDependencyReducedPom>false</createDependencyReducedPom>
          <filters>
            <filter>
              <artifact>*:*</artifact>
              <excludes>
                <exclude>META-INF/*.MF</exclude>
              </excludes>
            </filter>
          </filters>
          <relocations>
            <relocation>
              <pattern>dev.dejvokep.boostedyaml</pattern>
              <shadedPattern>de.tubyoub.velocitypteropower.libs</shadedPattern>
            </relocation>
          </relocations>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Compile to Java 21 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler.plugin.version}</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <release>${java.version}</release>
        </configuration>
      </plugin>

      <!-- Filter sources where needed -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>templating-maven-plugin</artifactId>
        <version>${templating.plugin.version}</version>
        <executions>
          <execution>
            <id>filter-src</id>
            <goals>
              <goal>filter-sources</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Create normal and API classifier jars -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.3.0</version>
        <executions>
          <execution>
            <id>default-jar</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
          <execution>
            <id>api-jar</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <classifier>api</classifier>
              <includes>
                <include>de/tubyoub/vpp/api/**</include>
              </includes>
              <archive>
                <manifestEntries>
                  <Automatic-Module-Name>de.tubyoub.vpp.api</Automatic-Module-Name>
                  <Built-By>VelocityPteroPower</Built-By>
                </manifestEntries>
              </archive>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Attach standard sources for main artifact AND api-only sources as extra classifier -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>${source.plugin.version}</version>
        <executions>
          <execution>
            <id>attach-main-sources</id>
            <phase>package</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
          <execution>
            <id>attach-api-sources</id>
            <phase>package</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
            <configuration>
              <classifier>api-sources</classifier>
              <includes>
                <include>de/tubyoub/vpp/api/**</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Attach standard javadoc for main artifact AND api-only javadoc as extra classifier -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>${javadoc.plugin.version}</version>
        <executions>
          <execution>
            <id>attach-main-javadoc</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <source>21</source>
              <quiet>true</quiet>
              <additionalJOption>-Xdoclint:none</additionalJOption>
            </configuration>
          </execution>
          <execution>
            <id>attach-api-javadoc</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <classifier>api-javadoc</classifier>
              <source>21</source>
              <quiet>true</quiet>
              <additionalJOption>-Xdoclint:none</additionalJOption>
              <excludePackageNames>de.tubyoub.vpp.internal:de.tubyoub.vpp.impl</excludePackageNames>
              <groups>
                <group>
                  <title>Public API</title>
                  <packages>de.tubyoub.vpp.api*</packages>
                </group>
              </groups>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Deploy using altDeploymentRepository from CI -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${deploy.plugin.version}</version>
      </plugin>

      <!-- Site/release plugins kept (not essential to deploy) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-site-plugin</artifactId>
        <version>${site.plugin.version}</version>
        <dependencies>
          <dependency>
            <groupId>net.trajano.wagon</groupId>
            <artifactId>wagon-git</artifactId>
            <version>${wagon.git.version}</version>
          </dependency>
          <dependency>
            <groupId>org.apache.maven.doxia</groupId>
            <artifactId>doxia-module-markdown</artifactId>
            <version>${doxia.markdown.version}</version>
          </dependency>
        </dependencies>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-release-plugin</artifactId>
        <version>${release.plugin.version}</version>
        <configuration>
          <autoVersionSubmodules>true</autoVersionSubmodules>
          <tagNameFormat>@{project.version}</tagNameFormat>
          <scmCommentPrefix xml:space="preserve">[RELEASE] </scmCommentPrefix>
          <goals>install deploy site-deploy</goals>
          <releaseProfiles>release</releaseProfiles>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <distributionManagement>
    <repository>
      <id>central</id>
      <name>Maven Central Portal</name>
      <url>https://central.sonatype.com/api/v1/publisher</url>
    </repository>
  </distributionManagement>

  <repositories>
    <repository>
      <id>papermc-repo</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>com.velocitypowered</groupId>
      <artifactId>velocity-api</artifactId>
      <version>${velocity.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>dev.dejvokep</groupId>
      <artifactId>boosted-yaml</artifactId>
      <version>${boostedyaml.version}</version>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.databind.version}</version>
    </dependency>

    <dependency>
      <groupId>net.kyori</groupId>
      <artifactId>adventure-text-minimessage</artifactId>
      <version>${minimessage.version}</version>
    </dependency>
  </dependencies>
</project>